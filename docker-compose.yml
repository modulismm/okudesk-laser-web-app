services:
  okudesk:
    build: .
    container_name: okudesk_laser_web
    restart: unless-stopped
    environment:
      - PORT=8000
      - DONATE_URL=https://buymeacoffee.com/bootlessbear
      - GITLAB_URL=https://gitlab.com/Bootlessbear/okudesk-laser-web-app
      - ENABLE_RASTER=1
      # - DEBUG=false
    ports:
      - "127.0.0.1:8000:8000"

